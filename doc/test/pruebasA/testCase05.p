// variables logicas, if, operador relacional =

program seis;

var     a:boolean;


begin


	write("IF VARIABLES LOGICAS");
	writeln();

        a:=true;
        write("true:");

        if (a <> false) then
                write("true");
        else
                write("false");
        

end.
